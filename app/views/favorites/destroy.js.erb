var postId = <%= @post.id %>
var newContent = "<%= escape_javascript(render partial: 'favorite_link', locals: { post: @post }) %>";

document.querySelectorAll('[data-favorite-info-id="' + postId + '"]').forEach(function(element) {
  element.innerHTML = newContent;
});
